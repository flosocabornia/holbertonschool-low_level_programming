/*
 * File: 2-starlen.c
 * Auth: Florencia Soca
 */
#include "holberton.h"

/**
* _strlen -a function that returns the length of a string
* @s: puntero
* Return: String length
*/
int _strlen(char *s)
{
int i;

for (i = 0; *(s + i) != '\0'; i++)
{
continue;
}
return (i);
}
